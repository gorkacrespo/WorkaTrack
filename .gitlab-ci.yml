# -------------------------------------------------------
# Pipeline CI para WorkaTrack (backend Flask)
# -------------------------------------------------------
# Objetivos:
# 1) Stage "test": comprobar que el código compila y que create_app() funciona.
# 2) Stage "build": construir y subir la imagen Docker al registry de GitLab
#    usando Kaniko.
#
# Más adelante podremos añadir:
# - Tests con pytest.
# - Despliegue automático a Kubernetes.
# -------------------------------------------------------

# Imagen base por defecto para los jobs que no la sobrescriban
image: python:3.12-slim

# Variables comunes a todos los jobs
variables:
  # Carpeta donde pip guardará la caché (para acelerar instalaciones)
  PIP_CACHE_DIR: "$CI_PROJECT_DIR/.cache/pip"

# Definimos las fases (stages) del pipeline
stages:
  - test
  - build
  - deploy

# Caché compartida entre jobs
cache:
  key: "${CI_COMMIT_REF_SLUG}"
  paths:
    - .cache/pip

# -------------------------------------------------------
# before_script global (solo se aplica al job test)
# -------------------------------------------------------
before_script:
  - python --version
  - pip install --upgrade pip
  - pip install -r requirements.txt

# -------------------------------------------------------
# Job: test_backend (STAGE: test)
# -------------------------------------------------------
test_backend:
  stage: test
  script:
    - python -m compileall app
    - python -c "from app import create_app; app = create_app(); print('create_app OK')"

# -------------------------------------------------------
# Job: build_image (STAGE: build)
# -------------------------------------------------------
build_image:
  stage: build

  image:
    name: gcr.io/kaniko-project/executor:debug
    entrypoint: [""]

  # Este job NO debe ejecutar el before_script global
  before_script: []

  script:
    # Crear configuración del registry
    - mkdir -p /kaniko/.docker
    - |
      cat <<EOF > /kaniko/.docker/config.json
      {
        "auths": {
          "${CI_REGISTRY}": {
            "username": "${CI_REGISTRY_USER}",
            "password": "${CI_REGISTRY_PASSWORD}"
          }
        }
      }
      EOF

    # Construir y subir imagen al registry
    - |
      /kaniko/executor \
        --context "${CI_PROJECT_DIR}" \
        --dockerfile "${CI_PROJECT_DIR}/Dockerfile" \
        --destination "${CI_REGISTRY_IMAGE}:${CI_COMMIT_REF_SLUG}" \
        --destination "${CI_REGISTRY_IMAGE}:latest"

# -------------------------------------------------------
# Job: deploy_to_k8s (STAGE: deploy)
# Aplica los manifiestos de k8s/ usando el kubeconfig del SA
# -------------------------------------------------------
deploy_to_k8s:
  stage: deploy
  image: bitnami/kubectl:latest

  script:
    - echo "$KUBECONFIG_WORKATRACK" > kubeconfig
    - kubectl --kubeconfig=kubeconfig apply -f k8s/
    - kubectl --kubeconfig=kubeconfig rollout status deployment/workatrack-api

  only:
    - master
