apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: workatrack-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
    - host: workatrack.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: workatrack-service
                port:
                  number: 80
